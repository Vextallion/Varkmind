/**
 * Product identity only — display copy lives in locales (`en-us.json`).
 * Change `name` here, then sync app.json / package.json if you rebrand.
 * Changing `storageId` resets MMKV on devices that already stored data.
 */
export const brand = {
  name: 'Varkmind',
  legalName: 'Varkmind',
  storageId: 'varkmind',
  scheme: 'varkmind',
} as const;
