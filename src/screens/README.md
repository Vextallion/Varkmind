# screens (FSD pages)

Full pages: Dashboard, Learn, Library, Profile. Wired from `src/app` routes.

## Rules

- May import from `@widgets`, `@features`, `@entities`, `@shared`.
- Keep screens thin — composition only.
- Expose via `screens/<Name>/index.ts`.

See [FSD.md](../FSD.md).
