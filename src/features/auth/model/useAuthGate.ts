/** @deprecated Prefer Stack.Protected in root layout; kept for callback edge cases. */
export function useAuthGate(): void {
  // Auth routing is handled by Stack.Protected + splash hold in app/_layout.
}
