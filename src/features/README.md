# features

Business scenarios: `upgrade-register`, `active-production`, `share-progress`.

## Rules

- May import from `@entities` and `@shared`.
- Must not import other features.
- Expose only slice root `index.ts`.

See [FSD.md](../FSD.md).
